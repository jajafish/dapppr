extends layout

block content

  section(class='user_profile')
    for png in user.shots
      img.user_pic.profile_center(src='#{png.image_url}')

    h2.user_name.profile_center
      | #{user.userName}
    h2 #{user.userLikes}
